Purpose:** Analyzes customer transaction data to determine average purchase intervals, favorite shopping days, and hours. **Inputs/Outputs:** Inputs: `input_pickle_file` (str): Path to the pickle file with customer transaction data.
Purpose:** Analyzes customer transaction data to determine average days between purchases, favorite shopping days, and hours. **Inputs/Outputs:** Inputs: `input_pickle_file` (str): Path to the pickle file containing customer transaction data.
Purpose:** Ensures that the 'CustomerID' and 'Hour' columns exist in the `result` DataFrame. **Inputs/Outputs:** No inputs; outputs an AssertionError if the columns are missing.
Purpose:** Load a DataFrame from a pickle file, remove rows with stock codes containing 0 or 1 numeric characters, and save the cleaned DataFrame back to a pickle file. **Inputs/Outputs:** Inputs: `input_pickle_path` (str, path to the input pickle file), `output_pickle_path` (str, path to the output pickle file).
Purpose**: Computes cancellation frequency and rates for customers based on transaction data. **Inputs/Outputs**: Inputs: `input_pickle_file` (str): Path to the pickle file containing transaction data.
Purpose**: The function processes a DataFrame by removing rows with certain service-related descriptions and standardizing the text to uppercase. **Inputs/Outputs**: Inputs: `input_pickle_path` (str, path to input pickle file), `output_pickle_path` (str, path to save the processed pickle file).
Purpose**: Computes the correlation matrix of a dataset and saves it as a heatmap image and a parquet file. **Inputs/Outputs**: Inputs: `in_path` (str): Path to the input parquet file.
Purpose**: Saves a heatmap figure to a file, creating directories if needed and handling file existence issues. **Inputs/Outputs**: Inputs: `fig` (matplotlib figure object to be saved), `path` (string specifying the file save location).
Purpose**: To persist a correlation matrix as a Parquet file at a given location, ensuring the directory exists and handling potential file conflicts. **Inputs/Outputs**: Inputs: `data` (expected to be a DataFrame), `path` (string specifying the file save location).
